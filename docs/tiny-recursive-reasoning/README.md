# TRM tutorial completion materials

The completed tutorial is `_posts/2025-10-14-tiny-recursive-reasoning.md`. The new method diagram is `assets/img/2025-tiny-recursive-reasoning/trm-gradient-boundary.svg`.

Working materials are under `_research/`, which Jekyll excludes from publication:

- `completion/verification.md`: completed scope and checks.
- `completion/edit-manifest.json`: hashes and offsets proving preservation of existing content.
- `completion/*sources.md`, `method-review.md`, and diagram provenance: sources and review notes.
- `official-code/`: pinned implementation snapshot.
- `papers/2510.04871v1/`: downloaded paper HTML, TeX sources, and figure inventory.

The later looped-Transformer addition has its own record under `_research/looped-transformers/`: primary-paper literature notes, LoopLM v5 source and figure provenance, the inserted section, and a separate preservation manifest. Its original architecture figure is stored as `looplm-architecture.png` and `looplm-architecture.pdf` alongside the post's other assets. The earlier `completion/` records describe the preceding tutorial-completion edit.
